import { createRule } from "../utils/createRule.js";

export default createRule({
  meta: {
    type: "problem",

    schema: [],

    messages: {},
  },

  defaultOptions: [],

  create: () => {
    return {};
  },
});
